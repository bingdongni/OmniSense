# OmniSense - Minimal Requirements
# 仅包含核心功能所需的依赖，适用于资源受限环境

# Core Dependencies (必需)
python-dotenv==1.0.1
loguru==0.7.2
pydantic==2.6.1
pydantic-settings==2.1.0
click==8.1.7

# HTTP & Scraping (必需)
requests==2.31.0
aiohttp==3.9.5
httpx==0.27.0
playwright==1.43.0
beautifulsoup4==4.12.3
lxml==5.3.0

# Anti-Crawl (必需)
fake-useragent==1.5.0

# Database (必需)
aiosqlite==0.19.0

# Data Processing (必需)
numpy==1.26.4
pandas==2.2.1

# Utilities (必需)
tqdm==4.66.2
python-dateutil==2.8.2
pytz==2024.1
pyyaml==6.0.1
aiofiles==23.2.1

# Text Processing (必需)
jieba==0.42.1

# Browser Cookie Import (必需)
browser-cookie3==0.19.1

# 可选：Web UI（运行streamlit时需要）
# streamlit==1.35.0

# 可选：API服务（运行API时需要）
# fastapi==0.110.0
# uvicorn[standard]==0.27.0
# python-multipart==0.0.9
# pyjwt==2.8.0
# passlib[bcrypt]==1.7.4

# 可选：AI功能（使用Agent时需要）
# langchain==0.1.20
# langchain-community==0.0.38
# langchain-core==0.1.52
# ollama==0.1.27

# 可选：向量数据库（使用ChromaDB时需要）
# chromadb==0.4.22

# 可选：Redis缓存
# redis==5.0.1

# 可选：代理支持
# python-socks[asyncio]==2.4.3
# aiohttp-socks==0.8.4
